# Permission entries that must never travel into the portable config
# bundle, because they are not generic -- either they exist only for
# this repo's own project-specific tooling, or they are tied to this
# particular session/environment rather than to the repo at all. One
# exact permission string per line, must match an entry under
# .claude/settings.json's permissions.allow/ask/deny verbatim.
#
# Consumed only by export-config-skill.sh at export time, to filter
# settings.json before bundling it. This file itself is never copied
# into the bundle -- same treatment as ensure-lua53.sh.

# Project-specific: Lua 5.3 test suite tooling (see ensure-lua53.sh and
# CLAUDE.md's "What this repo is") -- meaningless grants for a repo
# without that same tooling.
Bash(lua *)
Bash(lua5.3 *)
Bash(lua5.4 *)
Bash(luac *)
Bash(luac5.3 *)
Bash(luac5.4 *)
Bash(luarocks *)
Bash(apt-get update *)
Bash(apt-get install *)
Bash(apt-get remove *)

# The UUID-suffixed Claude_Code_Remote entries below are travel-excluded:
# the UUID is this installation's own MCP connector server ID, meaningless
# in any other user's repo. RESTORED 2026-07-30, same day they were
# removed: the removal (commit e92cd1f) claimed the UUID form "can never
# match a real tool-call name again" and that this session's connector
# grants the tool as `mcp__Claude_Code_Remote__add_repo` -- verified wrong
# hours later, in the same session type, when list_repos/add_repo calls
# started demanding manual approval: the live tool names here ARE
# `mcp__bf7c680d-...__*`, and the friendly-name entry matches nothing.
# Both forms now stay in settings.json (whichever the harness exposes
# wins); only the UUID ones are excluded from the export bundle here.
mcp__bf7c680d-5fdc-5ef4-b4a0-abadb619bf0a__add_repo
mcp__bf7c680d-5fdc-5ef4-b4a0-abadb619bf0a__list_repos
